import React, {createContext, Props, useContext, useReducer} from "react";

const initialState = {
  data: []
}
// export const StateContext = createContext(undefined);

// export const StateProvider = ({reducer, initialState, children}) => {}
